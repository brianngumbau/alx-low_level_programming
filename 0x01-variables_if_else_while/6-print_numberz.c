#include <stdio.h>
/**
 * main - a program that prints all single digit numbers of base 10
 * Return:0 (success)
 */
int main(void)
{
	int s;

	for (s = 0; s < 10 ; s++)
		putchar(s);
	putchar('\n');
	return (0);
}
