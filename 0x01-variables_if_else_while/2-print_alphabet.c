#include <stdio.h>
/**
 * main - a program that prints the alphabet in lower case
 * Return: 0 (success)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	return (0);
}
